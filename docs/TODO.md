Next steps/extensions
Planned improvements:
- Use GPT-4.1 nano to generate natural-language explanations for HO and stores.
- Add a simple “HO review queue” for warnings and hard stops.
- Deploy the app on a VPS and secure it behind a login.
- Swap TF–IDF similarity for embeddings once connected to production data.